/**
 * Profile types re-exported for backwards-compatibility.
 * No mock data: all profiles are real registered attendees in data/event_db.json.
 */
export type { Profile } from '$lib/types/profile';
export const MOCK_PROFILES = [];
